\sorts  {
    set;
}
\functions  {
    set finiteSetUnion(set, set);
    set finiteSetInter(set, set);
    set finiteSetMinus(set, set);
    boolean finiteSetMember(set, any);
    boolean finiteSetSubset(set, set);
    boolean finiteSetDisjunct(set, set);
    set finiteSetEmpty;
    set finiteSetSingleton(any);
    int finiteSetCard(set);
}

\javaSource ".";

\chooseContract
