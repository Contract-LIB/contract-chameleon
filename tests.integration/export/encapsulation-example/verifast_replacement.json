{
  "example.LinkedCellList.init": "head = null;",
  "example.LinkedCellList.add": "if (head == null) {\n          head = new NonEmptyLinkedCellList(v);\n        } else {\n          head.add(v);\n        }",
  "example.LinkedCellList.getLast": "Cell result; \n        if (head == null) { \n            result = null; \n        } else { \n            result = head.getLast(); \n        } \n        return result;",
  "example.Cell": "private int x;",
  "example.Cell.pred": "= x |-> absVal;",
  "example.Cell.init": "this.x = 0;",
  "example.Cell.value": "return this.x;",
  "example.Cell.set": "this.x = v;"
}
